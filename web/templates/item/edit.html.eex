<h2>Edit item</h2>

<%= render "form.html", changeset: @changeset,
                        action: locker_item_path(@conn, :update, @item) %>

<%= link "Back", to: locker_item_path(@conn, :index, @locker) %>
